const Checkout = ({ context }) => {
  // if (!isSignedIn) {
  //   router.push("/login");
  // } else {
  //   return <p>checkout details here</p>;
  // }

  return <p>checkout page</p>;
};

export default Checkout;
